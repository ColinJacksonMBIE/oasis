Feature: 1.2.5 Create SIO supervisors

	Internal users can add a SIO supervisor from existing contact records.
	
#@Manual	
#Scenario: add a new SIO supervisor
#	Given a internal user is adding a new SIO supervisor
#	When adding a user as SIO supervisor
#	Then user appears on SIO supervisors list
